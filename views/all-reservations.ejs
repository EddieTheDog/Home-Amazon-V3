<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>All Reservations</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>All Reservations</h1>
  <table border="1">
    <tr>
      <th>ID</th><th>Name</th><th>Status</th><th>Actions</th>
    </tr>
    <% reservations.forEach(r => { %>
    <tr>
      <td><%= r.id %></td>
      <td><%= r.name %></td>
      <td><%= r.status %></td>
      <td>
        <form action="/reservation-action/<%= r.id %>" method="POST" style="display:inline">
          <button name="action" value="cancel">Cancel</button>
          <button name="action" value="delay">Delay</button>
          <button name="action" value="early">Move Early</button>
          <button name="action" value="suspicious">Suspicious</button>
        </form>
      </td>
    </tr>
    <% }) %>
  </table>
  <br>
  <a href="/frontdesk">Back to Front Desk</a>
</body>
</html>
